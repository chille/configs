Private repository with configuration files used by me


### Installation
```bash
sudo apt-get install -y --no-install-recommends git
git clone https://github.com/chille/configs ~/configs
# Run the scripts in ~/configs/InstallScripts
```